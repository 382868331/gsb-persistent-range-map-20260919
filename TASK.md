# 完整首轮任务

给离线编辑器写一个区间样式库：编辑后能保留旧版本，也能找出两个版本之间需要重绘的范围。当前仓库只有骨架，需要从头实现下面的接口和行为。

从零实现int64半开区间[lo,hi)到字符串的不可变映射库。所有区间参数须lo<hi；空字符串是合法值，未覆盖与空字符串不同。端点允许MinInt64和MaxInt64，但MaxInt64点本身永不被覆盖；不使用hi+1转换。

Set覆盖区间，Erase撤销覆盖，均返回新版本；点查询显式返回是否覆盖，范围查询只返回覆盖片段并裁剪到查询范围，相邻同值段合并。旧版本保持可读，输入错误不改变旧版本。

使用持久化平衡有序树和结构共享，禁止每次全量复制。n为片段数，k为本次相交片段数，点查询O(log(n+1))、局部更新O((k+1)log(n+1))；允许AVL等常规方案，不要求并发写。记录实际新建/访问节点数。

Diff(old,new,lo,hi)按坐标返回差异；仅当相邻段的旧覆盖状态/值和新覆盖状态/值分别相同才合并。共享子树能判定相同时跳过。按结果对旧版本Set/Erase应重建查询范围内的新状态。

单版本最多10000片段，范围结果可线性输出。用[-16,16)逐点参考验证固定种子小更新序列、历史版本和Diff重建；用一次较大稀疏局部改动展示节点计数，不做长时间压力测试。

测试里请覆盖这些情况：覆盖切分、空值/空洞、相邻合并、不同旧新值对的相邻差异、int64端点、旧版本不变。

使用 Go 1.26.5 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `go run ./cmd/demo` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `go test ./... -count=1 -timeout=60s` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
