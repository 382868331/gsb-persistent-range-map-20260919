# 完整首轮任务

离线编辑器要保存不同版本的区间样式，并只重绘版本之间真正改变的范围。请从零实现“持久化区间覆盖与差异查询库”。

1. 从零实现int64半开区间[lo,hi)到字符串的不可变映射库。所有区间参数须lo<hi；空字符串是合法值，未覆盖与空字符串不同。端点允许MinInt64和MaxInt64，但MaxInt64点本身永不被覆盖；不使用hi+1转换。
2. Set覆盖区间，Erase撤销覆盖，均返回新版本；点查询显式返回是否覆盖，范围查询只返回覆盖片段并裁剪到查询范围，相邻同值段合并。旧版本保持可读，输入错误不改变旧版本。
3. 使用持久化平衡有序树和结构共享，禁止每次全量复制。n为片段数，k为本次相交片段数，点查询O(log(n+1))、局部更新O((k+1)log(n+1))；允许AVL等常规方案，不要求并发写。记录实际新建/访问节点数。
4. Diff(old,new,lo,hi)按坐标返回差异；仅当相邻段的旧覆盖状态/值和新覆盖状态/值分别相同才合并。共享子树能判定相同时跳过。按结果对旧版本Set/Erase应重建查询范围内的新状态。
5. 单版本最多10000片段，范围结果可线性输出。用[-16,16)逐点参考验证固定种子小更新序列、历史版本和Diff重建；用一次较大稀疏局部改动展示节点计数，不做长时间压力测试。

验收重点：覆盖切分、空值/空洞、相邻合并、不同旧新值对的相邻差异、int64端点、旧版本不变。

工程约束：全新0-1核心库，仅用Go 1.26.5标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`go run ./cmd/demo`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`go test ./... -count=1 -timeout=60s`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
