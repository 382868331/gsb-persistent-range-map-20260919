# 完整首轮任务

请从零实现“持久化区间覆盖与差异查询库”，用于存储引擎 / 持久化数据结构中的本地可复用模块。

1. 从零实现 int64 半开区间 [lo,hi) 到字符串的不可变映射库。空区间和反向区间拒绝，空字符串是合法值，未覆盖与空字符串不同；不允许用 hi+1 等可能溢出的转换。
2. Set 覆盖指定区间，Erase 撤销覆盖，每次返回新版本；点查询和范围查询按坐标递增返回最大连续等值片段，相邻等值段必须合并。旧版本永久保持可读。
3. 用平衡搜索树或等价有序树实现结构共享，禁止每次全量复制所有区间。公开更新新建节点数和查询访问节点数，计数必须来自实际访问；点查询应为 O(log n)，局部改动代价与相交片段及树高相关。
4. Diff(old,new,lo,hi) 返回值发生变化的最大半开片段，分别标明旧新覆盖状态与值；共享子树应跳过。结果可按顺序应用于旧版本重建查询范围内的新状态。
5. 输入检查失败不产生部分更新；可同时读取多个版本，不提供可变全局当前版本或后台线程。用小整数坐标上的逐点映射作独立参考验证固定种子的更新和差异序列。

验收重点：覆盖后切分、相邻合并、删除形成空洞、同值幂等更新、int64 两端、旧版本不变、Diff 重建。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Go 1.26.5标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；go run ./cmd/demo须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为go test ./... -count=1 -timeout=60s。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
